
export class InfoResumen{
    Total:number = 0;
    NumeroRepeticionesActual:number = 0;
    TotalAprendidas:number = 0;
    RepetidasConocidas = [0, 0, 0, 0, 0];
    RepetidasNoConocidas = [0, 0, 0, 0, 0];
    CodigoUsuario:number;// = $rootScope.globals.currentUser.username
    PendientesPorSubir:number;
}